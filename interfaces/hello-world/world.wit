package chikoski:hello;   

interface greet {
    hello: func() -> string;
    greet: func(name: string) -> string;
}

use chikoski:name/name-provider;

/// An example world for the component to target.
world example {
    import name-provider;
    export greet;
}